name: Validate

on:
  push:
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: Repository privacy and readiness check
        run: python scripts/validate_repo.py
      - name: Known-good A2UI sequence
        run: python scripts/validate_sequence.py examples/a2ui-good.json
      - name: Known-good reset and URL sequences
        run: |
          python scripts/validate_sequence.py examples/a2ui-reset-good.json
          python scripts/validate_sequence.py examples/url-good.json
      - name: Unit tests
        run: python -m unittest discover -s tests -v
      - name: Known-bad ordering must be rejected
        shell: bash
        run: |
          if python scripts/validate_sequence.py examples/a2ui-bad-present-first.json; then
            echo "validator unexpectedly accepted the bad sequence"
            exit 1
          fi
